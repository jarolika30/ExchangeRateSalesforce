export default class RateForSelectedDate {
    constructor(id, selectedDate, currencies) {
        this.id = id;
        this.selectedDate = selectedDate;
        this.currencies = currencies;
    }
}